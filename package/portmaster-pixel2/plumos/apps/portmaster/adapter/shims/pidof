#!/bin/sh
set -eu

case "${1:-}" in
  gptokeyb|gptokeyb2)
    exec "${PLUMOS_ROOT:-/mnt/plumos}/bin/plumos-portmaster-gptokeyb-stop" --list "$1"
    ;;
  *)
    exec "${PLUMOS_ROOT:-/mnt/plumos}/bin/busybox" pidof "$@"
    ;;
esac

#!/bin/bash
# Platform options sourced by PortMaster ports when CFW_NAME is plumOS.

PORT_32BIT="N"
GODOT2_OPTS="-r ${DISPLAY_WIDTH}x${DISPLAY_HEIGHT} -f"
GODOT_OPTS="--resolution ${DISPLAY_WIDTH}x${DISPLAY_HEIGHT} -f"

pm_platform_helper() {
  if [ -e "${PM_PIPE:-}" ]; then
    PortMasterDialogExit
  fi
}

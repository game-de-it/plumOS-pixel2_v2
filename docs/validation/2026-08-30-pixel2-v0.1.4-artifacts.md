# Pixel2 v0.1.4 release artifact preparation

Date: 2026-08-30

Release source: `5dcf4a35a38692e337f7d864d966bbd77ead7c8a`

## SD image

The release workflow ran from a clean worktree with:

```text
./scripts/prepare-pixel2-release.sh --version 0.1.4
```

The strict app layer, stock 5.10.198 SYSTEM, first-boot provisioning, license,
release-content, and implementation gates passed with zero release blockers.
The image was built independently twice and produced the same SHA-256:

```text
0a335affb12807772c3b70adcf9751e473a9409273df52a1cd720b507dcb2536
```

The compressed image archive passed a full `7zz t` readback. Its SHA-256 is:

```text
0f93466ba06a1c5477879639bcba6e65b2e4b68e40978a7b688344650f978728
```

## Runtime update

The signed Runtime update uses the exact public v0.1.3 app-layer checksum index
as its base. Its manifest records source `0.1.3`, target `0.1.4`, 56 changed
managed files, zero deletions, and 44,714,395 uncompressed payload bytes.

```text
57338318cff18a41f8e6db29d8dc64cb9024f54b052f415370187e93e04cb28f
```

The archive checksum, gzip stream, manifest metadata, and Ed25519 signature were
verified after copying the package into the final release directory.

## Final artifacts

The candidate bundle is in `dist/plumOS-Pixel2-v0.1.4/` and contains the SD
image archive, exact source archive, signed v0.1.3-to-v0.1.4 Runtime update,
release notes, provenance manifest, and checksums. `SHA256SUMS` and the repository
release-bundle verifier both passed.

## Acceptance boundary

The v0.1.4 PortMaster code path was deployed and validated on GKD Pixel2 hardware
before packaging, including non-black Rockbox rendering and clean frontend
return. The formal fresh-card image passed automated image, geometry, and
first-boot provisioning checks. A final operator boot from the newly generated
image and the proprietary Blaze of Storm title retest remain outside this
artifact-generation record. No GitHub tag, release, or asset upload was performed.


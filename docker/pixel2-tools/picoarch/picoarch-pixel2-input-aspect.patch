diff --git a/main.c b/main.c
index a2b5095..29c36eb 100644
--- a/main.c
+++ b/main.c
@@ -200,7 +200,7 @@ void set_defaults(void)
 	limit_frames = 1;
 	enable_audio = 1;
 	audio_buffer_size = 5;
-	scale_size = SCALE_SIZE_NONE;
+	scale_size = SCALE_SIZE_ASPECT;
 	// scale_filter = SCALE_FILTER_NEAREST;
 	scale_effect = default_scale_effect;
 	optimize_text = 1;
diff --git a/plat_linux.c b/plat_linux.c
index bc57f3c..cb25b1e 100644
--- a/plat_linux.c
+++ b/plat_linux.c
@@ -1,8 +1,10 @@
 #include <SDL/SDL.h>
+#include <linux/input.h>
 #include "libretro.h"
 #include "libpicofe/plat.h"
 #include "libpicofe/input.h"
 #include "libpicofe/in_sdl.h"
+#include "libpicofe/linux/in_evdev.h"
 #include "main.h"
 #include "util.h"
 
@@ -70,4 +72,56 @@ static const struct in_pdata in_sdl_platform_data = {
 	.modmap_size  = array_size(in_sdl_mod_keymap),
 };
 
+static const struct in_default_bind in_pixel2_evdev_defbinds[] = {
+	{ KEY_UP,     IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_UP },
+	{ KEY_DOWN,   IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_DOWN },
+	{ KEY_LEFT,   IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_LEFT },
+	{ KEY_RIGHT,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_RIGHT },
+	{ BTN_DPAD_UP,    IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_UP },
+	{ BTN_DPAD_DOWN,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_DOWN },
+	{ BTN_DPAD_LEFT,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_LEFT },
+	{ BTN_DPAD_RIGHT, IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_RIGHT },
+	{ BTN_SOUTH,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_B },
+	{ BTN_EAST,   IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_A },
+	{ BTN_NORTH,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_X },
+	{ BTN_WEST,   IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_Y },
+	{ BTN_TL,     IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_L },
+	{ BTN_TR,     IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_R },
+	{ BTN_TL2,    IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_L2 },
+	{ BTN_TR2,    IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_R2 },
+	{ BTN_SELECT, IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_SELECT },
+	{ BTN_START,  IN_BINDTYPE_PLAYER12, RETRO_DEVICE_ID_JOYPAD_START },
+	{ BTN_TRIGGER_HAPPY1, IN_BINDTYPE_EMU, EACTION_MENU },
+	{ 0, 0, 0 }
+};
+
+static const struct menu_keymap in_pixel2_evdev_key_map[] = {
+	{ KEY_UP,     PBTN_UP },
+	{ KEY_DOWN,   PBTN_DOWN },
+	{ KEY_LEFT,   PBTN_LEFT },
+	{ KEY_RIGHT,  PBTN_RIGHT },
+	{ BTN_DPAD_UP,    PBTN_UP },
+	{ BTN_DPAD_DOWN,  PBTN_DOWN },
+	{ BTN_DPAD_LEFT,  PBTN_LEFT },
+	{ BTN_DPAD_RIGHT, PBTN_RIGHT },
+	{ BTN_EAST,   PBTN_MOK },
+	{ BTN_SOUTH,  PBTN_MBACK },
+	{ BTN_WEST,   PBTN_MA2 },
+	{ BTN_NORTH,  PBTN_MA3 },
+	{ BTN_TL,     PBTN_L },
+	{ BTN_TR,     PBTN_R },
+	{ BTN_TRIGGER_HAPPY1, PBTN_MENU },
+};
+
+static const struct in_pdata in_pixel2_evdev_platform_data = {
+	.defbinds  = in_pixel2_evdev_defbinds,
+	.key_map   = in_pixel2_evdev_key_map,
+	.kmap_size = array_size(in_pixel2_evdev_key_map),
+};
+
+static int pixel2_evdev_init(void)
+{
+	return in_evdev_init(&in_pixel2_evdev_platform_data);
+}
+
 #include "plat_sdl.c"
diff --git a/plat_sdl.c b/plat_sdl.c
index 374d610..f33b50b 100644
--- a/plat_sdl.c
+++ b/plat_sdl.c
@@ -752,6 +752,11 @@ int plat_init(void)
 	g_menubg_src_h = SCREEN_HEIGHT;
 	g_menubg_src_pp = SCREEN_WIDTH;
 
+	if (pixel2_evdev_init()) {
+		PA_ERROR("Pixel2 evdev input failed to init\n");
+		return -1;
+	}
+
 	if (in_sdl_init(&in_sdl_platform_data, plat_sdl_event_handler)) {
 		PA_ERROR("SDL input failed to init: %s\n", SDL_GetError());
 		return -1;
diff --git a/libpicofe/linux/in_evdev.c b/libpicofe/linux/in_evdev.c
index ff714ce..7668b27 100644
--- a/libpicofe/linux/in_evdev.c
+++ b/libpicofe/linux/in_evdev.c
@@ -35,6 +35,8 @@ typedef struct {
 	int abs_lzone;
 	int abs_lastx;
 	int abs_lasty;
+	unsigned int has_hat0x:1;
+	unsigned int has_hat0y:1;
 	int kc_first;
 	int kc_last;
 	unsigned int abs_count;
@@ -58,6 +60,9 @@ typedef struct {
 #define KEYBITS_BIT_CLEAR(x) (keybits[(x)/sizeof(keybits[0])/8] &= \
 	~(1 << ((x) & (sizeof(keybits[0])*8-1))))
 
+#define ABSBITS_BIT(x) (absbits[(x)/sizeof(absbits[0])/8] & \
+	(1L << ((x) & (sizeof(absbits[0])*8-1))))
+
 int in_evdev_allow_abs_only;
 
 #define IN_EVDEV_PREFIX "evdev:"
@@ -244,6 +249,13 @@ static void in_evdev_probe(const in_drv_t *drv)
 				dev->abs_min_y = ainfo.minimum;
 				dev->abs_max_y = ainfo.maximum;
 			}
+			dev->has_hat0x = !!ABSBITS_BIT(ABS_HAT0X);
+			dev->has_hat0y = !!ABSBITS_BIT(ABS_HAT0Y);
+			if (dev->has_hat0x || dev->has_hat0y) {
+				/* Pixel2 adc_gamepad exposes its d-pad as HAT0X/HAT0Y. */
+				have_abs = 1;
+				dev->abs_to_digital = 1;
+			}
 			for (u = 0; u < MAX_ABS_DEVS; u++) {
 				ret = ioctl(fd, EVIOCGABS(u), &ainfo);
 				if (ret == -1)
@@ -365,6 +377,21 @@ static int in_evdev_update(void *drv_data, const int *binds, int *result)
 		}
 	}
 
+	if (dev->has_hat0x) {
+		ret = ioctl(dev->fd, EVIOCGABS(ABS_HAT0X), &ainfo);
+		if (ret != -1) {
+			if (ainfo.value < 0) or_binds(binds, KEY_LEFT, result);
+			if (ainfo.value > 0) or_binds(binds, KEY_RIGHT, result);
+		}
+	}
+	if (dev->has_hat0y) {
+		ret = ioctl(dev->fd, EVIOCGABS(ABS_HAT0Y), &ainfo);
+		if (ret != -1) {
+			if (ainfo.value < 0) or_binds(binds, KEY_UP, result);
+			if (ainfo.value > 0) or_binds(binds, KEY_DOWN, result);
+		}
+	}
+
 	return 0;
 }
 
@@ -491,7 +518,15 @@ static int in_evdev_update_keycode(void *data, int *is_down)
 		int lzone = dev->abs_lzone, down = 0, *last;
 
 		// map absolute to up/down/left/right
-		if (lzone != 0 && ev.code == ABS_X) {
+		if (ev.code == ABS_HAT0X) {
+			down = ev.value < 0 ? KEY_LEFT : ev.value > 0 ? KEY_RIGHT : 0;
+			last = &dev->abs_lastx;
+		}
+		else if (ev.code == ABS_HAT0Y) {
+			down = ev.value < 0 ? KEY_UP : ev.value > 0 ? KEY_DOWN : 0;
+			last = &dev->abs_lasty;
+		}
+		else if (lzone != 0 && ev.code == ABS_X) {
 			if (ev.value < dev->abs_min_x + lzone)
 				down = KEY_LEFT;
 			else if (ev.value > dev->abs_max_x - lzone)
@@ -592,6 +627,12 @@ static int in_evdev_clean_binds(void *drv_data, int *binds, int *def_binds)
 		KEYBITS_BIT_SET(KEY_UP);
 		KEYBITS_BIT_SET(KEY_DOWN);
 	}
+	if (dev->has_hat0x || dev->has_hat0y) {
+		KEYBITS_BIT_SET(KEY_LEFT);
+		KEYBITS_BIT_SET(KEY_RIGHT);
+		KEYBITS_BIT_SET(KEY_UP);
+		KEYBITS_BIT_SET(KEY_DOWN);
+	}
 
 	for (i = 0; i < KEY_CNT; i++) {
 		for (t = 0; t < IN_BINDTYPE_COUNT; t++) {
